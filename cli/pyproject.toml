[project]
name = "apit"
version = "0.0.0"
description = "api-tracker CLI client (placeholder for M1, see specs/implementation-plan.md §3)"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/apit"]
