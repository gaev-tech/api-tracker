[project]
name = "auth-service"
version = "0.0.0"
description = "api-tracker auth service (placeholder for M2, see specs/implementation-plan.md §4)"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/auth_service"]
