replicaCount: 1

image:
  repository: ghcr.io/gaev-tech/api-tracker/identity-service
  tag: latest
  pullPolicy: IfNotPresent

service:
  port: 8080
  grpcPort: 9090

hpa:
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70

ingress:
  enabled: false

# JWT_PRIVATE_KEY comes from identity-jwt-secret via envFrom.secretRef below.
# DB_PASSWORD is read from identity-db-credentials and interpolated into DATABASE_URL
# using Kubernetes env var substitution syntax: $(VAR_NAME).
env:
  - name: PORT
    value: "8080"
  - name: JWT_ISSUER
    value: "api-tracker"
  - name: APP_BASE_URL
    value: "https://apitracker.ru"
  - name: DB_PASSWORD
    valueFrom:
      secretKeyRef:
        name: identity-db-credentials
        key: password
  - name: SMTP_HOST
    valueFrom:
      secretKeyRef:
        name: identity-smtp-credentials
        key: SMTP_HOST
  - name: SMTP_PORT
    valueFrom:
      secretKeyRef:
        name: identity-smtp-credentials
        key: SMTP_PORT
  - name: SMTP_FROM
    valueFrom:
      secretKeyRef:
        name: identity-smtp-credentials
        key: SMTP_FROM
  - name: SMTP_PASSWORD
    valueFrom:
      secretKeyRef:
        name: identity-smtp-credentials
        key: SMTP_PASSWORD

# JWT_PRIVATE_KEY is loaded from the identity-jwt-secret Secret (created in cd-helm.yml)
envFrom:
  configMapRef: ""
  secretRef: "identity-jwt-secret"

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

serviceMonitor:
  enabled: false
  interval: 30s
  path: /metrics
