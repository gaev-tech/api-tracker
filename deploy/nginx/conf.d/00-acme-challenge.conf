# ACME HTTP-01 challenge — обслуживается nginx без редиректа.
# Применяется ко всем :80 хостам через include в их server-блоках,
# но certbot --webroot пишет в общий каталог, так что мы можем
# обслуживать challenge с любого хоста.
# Этот файл намеренно не содержит server-блок — он подключается
# в конкретные хосты через include.
